# Keep the image small and secrets OUT of it. .env is passed at runtime (env_file),
# never baked into the image.
.env
.env.*
!.env.example
node_modules
dist
.git
.astro
feedback/*.md
feedback/screenshots/*
# heavy source-data / scrape artefacts not needed to build the site
*.csv
halodoc_*.json
*.pharmacy.pdf
_sensitive/
content%3A*
Tourist Pharmacy Logo.png
npm-debug.log*
.DS_Store
